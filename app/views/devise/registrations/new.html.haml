%div.centered.signup-form
  %h3 Sign up

  Sign up with your account from
  = auth_providers_links

  Or create a new one
  = form_for resource, :as => resource_name, :url => registration_path(resource_name), :html => { :class => 'nice' } do |f|

    %div.field-wrapper
      = error_message resource, :name
      = f.text_field :name, :class => 'input-text', :placeholder => 'Name'

    %div.field-wrapper
      = error_message resource, :email
      = f.email_field :email, :class => 'input-text', :placeholder => 'Email'

    %div.field-wrapper
      = error_message resource, :password
      = f.password_field :password, :class => 'input-text', :placeholder => 'Password'

    = f.password_field :password_confirmation, :class => 'input-text', :placeholder => 'Password confirmation'

    %div.submit
      = f.submit "Sign up", :class => 'white nice button radius'

  %div.note
    Please, don't use this form to add new OpenID providers to your Taste-o-Code account if you already have one.
